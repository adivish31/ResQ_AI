datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL") // Uses the Pooler for the App
  directUrl = env("DIRECT_URL") // Uses Direct connection for Migrations
}

generator client {
  provider = "prisma-client-js"
}

model HelpRequest {
    id String @id @default(uuid())
}